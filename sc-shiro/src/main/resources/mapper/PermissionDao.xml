<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springcloud.shiro.scshiro.dao.PermissionDao">

    <resultMap type="com.springcloud.shiro.scshiro.entity.Permission" id="PermissionMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="alias" column="alias" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="pid" column="pid" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="PermissionMap">
        select id,
               alias,
               create_time,
               name,
               pid
        from permission
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="PermissionMap">
        select id,
               alias,
               create_time,
               name,
               pid
        from permission limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="PermissionMap">
        select
        id, alias, create_time, name, pid
        from permission
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="alias != null and alias != ''">
                and alias = #{alias}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="pid != null">
                and pid = #{pid}
            </if>
        </where>
    </select>
    <select id="queryByRoleId" resultMap="PermissionMap">
        select p.*
        from permission p, roles_permissions rp
        where p.id = rp.permission_id and rp.role_id = #{roleId}
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into permission(alias, create_time, name, pid)
        values (#{alias}, #{createTime}, #{name}, #{pid})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update permission
        <set>
            <if test="alias != null and alias != ''">
                alias = #{alias},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="pid != null">
                pid = #{pid},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from permission
        where id = #{id}
    </delete>

</mapper>